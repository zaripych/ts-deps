language: node_js
cache:
  yarn: true
  directories:
    - node_modules
node_js:
  - '8'
branches:
  except:
    - /^v\d+\.\d+\.\d+$/
stages:
  - name: test and lint
  - name: deploy
    if: branch = master AND type != pull_request
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH="$HOME/.yarn/bin:$PATH"
after_success:
  - cat coverage/lcov.info | ./node_modules/.bin/coveralls
jobs:
  include:
    - stage: test and lint
      script:
        - time yarn run check
    - stage: test and lint
      script:
        - time yarn run test
        - time yarn integration-all
        - time yarn combine-coverage
    - stage: deploy
      script:
        - yarn build
      deploy:
        provider: script
        script:
          - yarn run release
        skip_cleanup: true
        on:
          branch:
            - master
